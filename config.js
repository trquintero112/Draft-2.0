window.SUPABASE_URL = window.SUPABASE_URL || '';
window.SUPABASE_PUBLIC_KEY = window.SUPABASE_PUBLIC_KEY || '';
window.SUPABASE_TABLE = window.SUPABASE_TABLE || 'fantasy_players';
// Optional: paste a FantasyPros API key here if you want the i button to attempt live FantasyPros rankings data.
// Note: on GitHub Pages, this key is visible in the browser. For a private key, use a backend/proxy instead.
window.FANTASYPROS_API_KEY = window.FANTASYPROS_API_KEY || '';
